/* app.js — shared utilities, no page-specific logic */

/* Escape HTML to prevent XSS when building DOM strings */
function escHtml(s) {
  return String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
